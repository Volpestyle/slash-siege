import { MainSceneConfig } from "../types/main-scene-types";

// Default config values
export const MAIN_SCENE_CONFIG: MainSceneConfig = {
  joyStickDebug: true,
  playerDebug: true,
  showFPS: true,
};
